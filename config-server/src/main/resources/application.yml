server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/shaanzx/Smart-Parking-config-server.git
          default-label: main

management:
  endpoints:
    web:
      exposure:
        include: "*"